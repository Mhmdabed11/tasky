import styled from '@emotion/styled';
import { space, color, layout, flexbox, border, typography } from 'styled-system';

const Box = styled.div(space, color, layout, flexbox, border, typography);
export default Box;
